import React, { Component } from 'react';
import { connect } from 'react-redux';
import './style.css';

class Login extends Component {

  render() {

    return (
      <div>
        GOOOOOOOOD Performer!!!!!
      </div>
    );
  }
}

export default connect(
  state => ({}),
  dispatch => ({})
)(Login);
